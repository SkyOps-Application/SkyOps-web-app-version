databases:
  - name: skyops-db
    databaseName: skyops_db
    user: user
    plan: free
    region: singapore 

  - name: skyops-redis
    plan: free
    region: singapore

services:
  # -------------------------
  # USER SERVICE (Python)
  # -------------------------
  - type: web
    name: skyops-user-service
    env: docker
    rootDir: atc-radar-sim/backend/user_service
    plan: free
    region: singapore
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: skyops-db
          property: connectionString
      - key: REDIS_URL
        fromDatabase:
          name: skyops-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production

  # -------------------------
  # SIMULATOR SERVICE (Node)
  # -------------------------
  - type: web
    name: skyops-simulator-service
    env: docker
    # Root dir is project root because it needs 'shared' folder context
    rootDir: atc-radar-sim
    dockerfilePath: backend/simulator_service/Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 4000
      - key: REDIS_URL
        fromDatabase:
          name: skyops-redis
          property: connectionString
      - key: CORS_ORIGIN
        value: "*" # Set to * initially to avoid circular dependency. Secure this later.
      - key: CACHE_BUSTER
        value: "1" # Force blueprint update
      - key: JWT_SECRET_KEY
        generateValue: true

  # -------------------------
  # NOTIFICATION SERVICE (Node)
  # -------------------------
  - type: web
    name: skyops-notification-service
    env: docker
    rootDir: atc-radar-sim
    dockerfilePath: backend/notification_service/Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 8080
      - key: REDIS_URL
        fromDatabase:
          name: skyops-redis
          property: connectionString
      # Add SMTP details in Render Dashboard manually for security
      - key: MAIL_SERVER
        value: smtp.example.com
      - key: SMTP_HOST
        value: smtp.gmail.com

  # -------------------------
  # FRONTEND (Next.js)
  # -------------------------
  - type: web
    name: skyops-frontend
    env: docker
    rootDir: atc-radar-sim
    dockerfilePath: frontend/Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: "/api" # Use relative path to trigger Next.js rewrite
      - key: INTERNAL_API_URL
        fromService:
          type: web
          name: skyops-user-service
          property: hostport # Internal host:port for server-side proxy
      - key: NEXT_PUBLIC_SIMULATOR_URL
        fromService:
          type: web
          name: skyops-simulator-service
          property: host
