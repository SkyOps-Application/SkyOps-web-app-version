{
  "name": "@atc-radar-sim/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run postbuild",
    "postbuild": "node -e \"const fs=require('fs');const path=require('path');function createBridge(src,dist){const dir=path.dirname(src);fs.mkdirSync(dir,{recursive:true});const rel=path.relative(dir,dist);fs.writeFileSync(src,'module.exports=require('+JSON.stringify(rel)+');\\n');}createBridge('src/data/exercises.js','dist/data/exercises.js');createBridge('src/data/waypoints.js','dist/data/waypoints.js');\"",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}

